{
  "name": "streamer",
  "version": "0.0.1",
  "description": "A wrapper around event emitters or promises to expose events as a recursively defined stream with a current known value and a later stream",
  "main": "streamer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adrien Cransac",
  "license": "ISC"
}
